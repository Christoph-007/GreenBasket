import 'package:flutter/material.dart';
class AdminMerchantsScreen extends StatelessWidget {
  const AdminMerchantsScreen({super.key});
  @override
  Widget build(BuildContext context) => Scaffold(body: Center(child: Text('Merchants')));
}